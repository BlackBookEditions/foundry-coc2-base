<header class="release-notes-header">
  <h1>{{localize "COC2BASE.releaseNotes.heading"}}</h1>
  <p class="subtitle">{{localize "COC2BASE.releaseNotes.subtitle" version=version}}</p>
</header>

<section class="release-notes-body">
  {{#each entries}}
    <article class="release-note">
      <h2 class="release-note-version">{{this.version}}</h2>
      <div class="release-note-content">{{{this.html}}}</div>
    </article>
  {{else}}
    <p class="empty">{{localize "COC2BASE.releaseNotes.empty"}}</p>
  {{/each}}
</section>

<footer class="release-notes-footer">
  <label class="dont-show-again" data-tooltip="{{localize 'COC2BASE.releaseNotes.dontShowAgainHint'}}">
    <input type="checkbox" name="dontShowAgain" {{checked dontShowAgain}} />
    {{localize "COC2BASE.releaseNotes.dontShowAgain"}}
  </label>
</footer>
